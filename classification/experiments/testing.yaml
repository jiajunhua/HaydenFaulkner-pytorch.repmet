run_type: 'magnetloss'
run_id: 'tester-01'
seed: 7

model:
  type: 'protonet'
  id: '01'
  use_pretrained: False


dataset:
  name: 'omniglot'
  id: '00'


train:
  sampler: 'magnet'
  loss: 'magnet'

  checkpoint_every: 10  # this many epochs

  epochs: 100
  learning_rate: 0.001
  lr_scheduler_gamma: 0.5
  lr_scheduler_step: 20

  episodes: 1  # per epoch
  k: 1
  m: 12
  d: 4


val:
  every: 1000  # run validation every x epochs

  sampler: 'magnet'
  loss: 'magnet'

  episodes: 1  # per epoch
  categories_per_epi: 5
  support_per_epi: 5
  query_per_epi: 15


test:  # will only do one epoch
  split: 'test'

  resume_from: 'B'

  sampler: 'magnet'
  loss: 'magnet'

  episodes: 500
  categories_per_epi: 5
  support_per_epi: 5
  query_per_epi: 15


vis:
  every: 10  # update tensorboard every x steps (episodes)
  plot_embed_every: 500  # update tensorboard projector every x steps (episodes) (Train and Val)
  test_plot_embed_every: 100  # update tensorboard projector every x steps (episodes) (Test)